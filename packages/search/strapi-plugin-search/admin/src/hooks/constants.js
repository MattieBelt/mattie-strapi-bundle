import pluginId from '../pluginId';

export const IS_LOADING_SETTINGS = `${pluginId}/settings/is-loading`;
export const RESOLVE_SETTINGS = `${pluginId}/settings/resolve`;
export const UPDATE_SETTINGS = `${pluginId}/settings/update`;
