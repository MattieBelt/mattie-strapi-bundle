'use strict';

module.exports = {
  /**
   * @returns {object} Default plugin configuration
   */
  default: () => ({}),
  /**
   * @returns {object} Configuration validator
   */
  validator: () => ({}),
};
